# ─────────────────────────────────────────────────────────────────────────────
# airflow/requirements.txt — Apache Airflow 3.x / Python 3.12
#
# NOTA: apache-airflow-providers-amazon excluido intencionalmente.
# Motivo: arrastra sqlalchemy-redshift que requiere SQLAlchemy <2.0,
#         incompatible con Airflow 3.x (requiere SQLAlchemy >=2.0).
# El stack lakeforge usa MinIO via boto3 directo, sin provider AWS completo.
# ─────────────────────────────────────────────────────────────────────────────

apache-airflow==3.2.2

# Providers necesarios para lakeforge
apache-airflow-providers-apache-spark==4.8.0
apache-airflow-providers-apache-kafka==1.3.0
apache-airflow-providers-microsoft-mssql==3.6.0
apache-airflow-providers-apache-hive==8.2.0

# MinIO: conexión directa vía boto3 (sin sqlalchemy-redshift)
boto3==1.34.0
botocore==1.34.0

# Secretos (OpenBao / Vault API compatible)
hvac==2.3.0

# Testing
pytest==8.2.0
pytest-cov==5.0.0

# Calidad de código
ruff==0.4.4
detect-secrets==1.5.0
