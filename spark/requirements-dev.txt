# Herramientas de desarrollo de los jobs Spark — venv local, no se instalan
# en las imágenes de spark-master ni spark-worker.
#
#   pip install -r spark/requirements-dev.txt
#
# Mantenerlas fuera del entorno de ejecución evita que un CVE de una
# herramienta de test aparezca como vulnerabilidad de producción.

pytest==9.1.1
pytest-cov==5.0.0
ruff==0.4.4

# Comparación de DataFrames en tests. Sin uso todavía: los tests actuales cubren
# spark/jobs/transformaciones.py, que es Python puro y no necesita una sesión de
# Spark. chispa espera a los tests de la escritura en Delta, que sí la necesitan.
# No arrastra pyspark —solo prettytable—, así que el job del CI sigue ligero.
# Soporta PySpark 3.5.x / 4.0.x / 4.1.x y Python 3.10–3.12.
chispa==0.12.0
